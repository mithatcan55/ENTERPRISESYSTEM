import { PageHeader } from "@/components/ui/PageHeader";

export default function RolesListPage() {
  return (
    <div>
      <PageHeader title="Rol Yönetimi" subtitle="Sistem rol tanımları — SYS_ADMIN" />
      <p className="text-[13px]" style={{ color: "#7A96B0" }}>Sayfa hazırlanıyor.</p>
    </div>
  );
}
